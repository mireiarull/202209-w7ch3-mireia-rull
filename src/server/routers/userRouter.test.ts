describe("Given a POST method with /users/register endpoint", () => {
  describe("When it receives a request with username 'mireia' and password '1234'", () => {
    test("Then it should respond with a 201 status and a new user mireia", () => {});
  });
});
